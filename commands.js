

const viewDepartments = () => {
    `SELECT FROM * departments;`
};

const viewRoles = () => {
    `SELECT FROM * roles;`
};

const viewEmployees = () => {
    `SELECT FROM * employees;`
};

const addDepartment = () => {
    
}